export type TfWorkfold = {
    collection: string
}
  
export type TfInfo = {
    localInformation: {
        serverPath: string
    }
}

export type Command = {
    title: string
    detail?: string
    command: string
  }
  